experiment_name: ag_news_instructor_v6_simple_test
dataset:
  name: ag_news
  split: test
  text_column: text
  label_column: label
  max_samples: 1000
task:
  label_mode: l2
models:
  biencoder:
    name: hkunlp/instructor-base
pipeline:
  normalize_embeddings: true
  random_seed: 42
output:
  output_dir: scripts/prompt_engineering/results
  save_metrics: true
  save_predictions: true