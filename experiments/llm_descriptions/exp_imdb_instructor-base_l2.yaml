experiment_name: exp_imdb_instructor-base_l2
dataset:
  name: imdb
  split: test
  text_column: text
  label_column: label
  max_samples: 2000
task:
  label_mode: l2
models:
  biencoder:
    name: hkunlp/instructor-base
pipeline:
  normalize_embeddings: true
output:
  output_dir: results
  save_metrics: true
  save_predictions: false
