experiment_name: exp_yahoo_answers_topics_instructor-base_l2
dataset:
  name: yahoo_answers_topics
  split: test
  text_column: text
  label_column: label
task:
  label_mode: l2
models:
  biencoder:
    name: hkunlp/instructor-base
pipeline:
  normalize_embeddings: true
output:
  output_dir: results
  save_metrics: true
  save_predictions: false
